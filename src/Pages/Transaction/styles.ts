import styled from "styled-components";

export const TransactionPage = styled.main``;

export const Form = styled.form``;

export const PageTitle = styled.h1``;

export const FormTitle = styled.h2``;
